spring:
  application:
    name: araok-configuration-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Maxim1234567/araok-config-repo/
          search-paths: "{application}"

server:
  port: 5000